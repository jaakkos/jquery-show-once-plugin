# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'coffeescript', :input => 'src', :output => 'lib'

# guard :shell do
# watch(%r{^lib/.+\.js$})   { `buster-test` }
# watch(%r{^test/.+\.js$})  { `buster-test` }
# end
